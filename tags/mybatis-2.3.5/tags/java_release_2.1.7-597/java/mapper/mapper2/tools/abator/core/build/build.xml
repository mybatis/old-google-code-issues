<project name="Abator for iBATIS" default="buildAbator" basedir=".">

  <!-- this is not finished yet - still building the distro manually -->
  
  <target name="clean">
  </target>
  
  <target name="buildAbator">
    <mkdir dir="${basedir}/deploy/bin"/>
    <javac srcdir="../src" destdir="${basedir}/deploy/bin" deprecation="true" debug="true" />
  </target>
  
</project>